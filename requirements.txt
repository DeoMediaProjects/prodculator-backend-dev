fastapi>=0.115.0
uvicorn[standard]>=0.27.0
pydantic[email]>=2.6.0
pydantic-settings>=2.1.0
sqlmodel>=0.0.22
SQLAlchemy>=2.0.0
alembic>=1.14.0
asyncpg>=0.30.0
aiosqlite>=0.20.0
psycopg2-binary>=2.9.0
redis>=5.2.0
rq>=1.16.0
authlib>=1.4.0
passlib>=1.7.4
argon2-cffi>=23.1.0
python-multipart>=0.0.9
httpx>=0.27.0
# Pinned exact, not a floor. Production hit "Messages.stream() got an
# unexpected keyword argument 'temperature'" — this floor (>=0.69.0) let
# pip resolve an older release whose streaming helper had a narrower
# signature than .create(). 0.123.0 is verified (inspect.signature) to
# accept `temperature` on both .create() and .stream(); pin it so a
# rebuild can't silently drift back to an incompatible version without a
# lock file.
anthropic==0.123.0
openai>=1.50.0
stripe>=8.0.0
Jinja2>=3.1.3
pdfplumber>=0.10.0
weasyprint>=62.0
pypdf>=4.0.0
reportlab>=4.0.0
APScheduler>=3.10.0
python-dotenv>=1.0.0
slowapi>=0.1.9
sentry-sdk[fastapi]>=2.0.0
firebase-admin>=6.4.0
boto3>=1.34.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
moto[s3]>=5.0.0
openpyxl>=3.1.0

# Account logo upload: decodes and re-encodes user-supplied images so the bytes
# served back are never the bytes uploaded. Previously present only as a
# transitive dependency, which is too load-bearing to leave unpinned.
Pillow==12.3.0
